// This Profile component has been replaced by ProfileModal.tsx
// The profile functionality is now accessible via a profile icon in the header
// that opens a popup-style modal instead of a dedicated page.

export default null;